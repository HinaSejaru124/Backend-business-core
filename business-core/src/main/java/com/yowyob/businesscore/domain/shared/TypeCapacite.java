package com.yowyob.businesscore.domain.shared;

/** Capacités activables et combinables d'une offre (modèle hybride socle + capacités). */
public enum TypeCapacite {
    STOCKABLE,
    PLANIFIABLE,
    RESERVABLE,
    RECURRENT
}
